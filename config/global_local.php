<?php 

define("DB_HOST","localhost");            # Ip de la pc servidor de base de datos

define("DB_NAME", "huacachina_admin");    # Nombre de la base de datos

define("DB_USERNAME", "root");            # Usuario de la base de datos

define("DB_PASSWORD", "");                # Contraseña del usuario de la base de datos

define("DB_ENCODE","utf8");               # definimos la codificación de los caracteres

define("PRO_NOMBRE","SISTEMA LANDIN JDL");#Definimos una constante como nombre del proyecto

// $config['base_url'] = ((isset($_SERVER['HTTPS']) && $_SERVER['HTTPS'] == "on") ? "https" : "http");
// $config['base_url'] .= "://".$_SERVER['HTTP_HOST'];
// $config['base_url'] .= str_replace(basename($_SERVER['SCRIPT_NAME']),"",$_SERVER['SCRIPT_NAME']);
// $server = $_SERVER['HTTP_HOST']; var_dump($server); 
// var_dump($config['base_url']); die;
?>